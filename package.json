{
  "private": true,
  "scripts": {
    "setup": "npm install && cd frontend && npm install && cd ../ai-service && python -m venv .venv && .venv\\Scripts\\python -m pip install -r requirements.txt",
    "dev": "concurrently -k -n be,fe,ai -c cyan,yellow,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai\"",
    "dev:backend": "cd backend/CogniLight.Api && dotnet run --launch-profile http",
    "dev:frontend": "cd frontend && npx ng serve",
    "dev:ai": "cd ai-service && .venv\\Scripts\\python -m uvicorn main:app --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
