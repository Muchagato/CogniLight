# --- CogniLight NAS Deployment ---

# GHCR image prefix (e.g., ghcr.io/yourusername/cognilight)
IMAGE_PREFIX=ghcr.io/OWNER/cognilight

# Allowed CORS origins (comma-separated, leave empty to allow frontend proxy only)
CORS_ORIGINS=https://cognilight.muchagato.dev
