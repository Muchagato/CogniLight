# CogniLight — Environment Configuration
# LLM API keys are now BYOK (Bring Your Own Key) — visitors enter
# their own key in the browser. No server-side key needed.

# Optional overrides:
# INGEST_INTERVAL=10
# DATABASE_PATH=../backend/CogniLight.Api/cognilight.db
# CORS_ORIGINS=https://cognilight.muchagato.dev
